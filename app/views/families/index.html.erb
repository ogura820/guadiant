<h2>必要備蓄量目安</h2>
<%= link_to "必要備蓄量をメールで受け取る", families_path(sent_mail: "true")%>
  <p>必要な生米の量は<%= @total_required_rice.to_i %>kg</p>
  <% @required_stock.each do |key, value| %>
    <p>必要な、<%= key %>は<%= value %>です。</p>
  <% end %>
  その他、調味料インスタント味噌汁や即席スープ、野菜ジュースがあるとよい
  https://www.maff.go.jp/j/zyukyu/foodstock/attach/pdf/guidebook-3.pdf
<h2>備蓄算出用メンバー</h2>
<%= render 'form' %> 現在メンバーは<%= @families.count %>人です
<table class="table table-striped">
  <thead>
    <tr>
      <th>名前</th>
      <th>年齢</th>
    </tr>
  </thead>
  <tbody>
    <% @families.each do |family| %>
      <tr>
        <td><%= family.name %></td>
        <td><%= image_tag family.icon_image, class: 'icon' %><%= family.age %>歳</td>
        <td>
          <%= link_to  edit_family_path(family), class: "btn btn-success" do %>
            <i class="bi bi-pencil-fill" aria-hidden="true"></i>編集
          <% end %>  
        </td>
        <td>
            <%= link_to  family, method: :delete, data: { confirm: '本当に削除しますか?' }, class: "btn btn-danger" do %>
              <i class="bi bi-x-lg" aria-hidden="true"></i>削除
            <% end %>  
        </td>
      </tr>
    <% end %>
  </tbody>
  </tbody>
</table>